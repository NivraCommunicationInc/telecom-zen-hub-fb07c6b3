/**
 * CoreStockPage — Redirects to the canonical EquipmentInventoryPage.
 * Both /core/stock and /core/equipment resolve to the same unified page.
 */
export { default } from "./EquipmentInventoryPage";
